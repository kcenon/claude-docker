# docker-compose.yml — Base config (Tier A: shared source)

services:
  claude-a:
    build:
      context: .
      args:
        CLAUDE_CODE_VERSION: ${CLAUDE_CODE_VERSION:-}
        WORKSPACE_DIR: ${WORKSPACE_DIR:-/workspace}
    image: claude-code-base:latest
    working_dir: ${WORKSPACE_DIR:-/workspace}
    stdin_open: true
    tty: true
    volumes:
      - ${PROJECT_DIR}:${WORKSPACE_DIR:-/workspace}
      - ${HOME}/.claude-state/account-a:/home/node/.claude
      - ${HOME}/.claude:/home/node/.claude-host:ro
      - ${HOME}/.config/gh:/home/node/.config/gh:ro
      - node_modules_a:${WORKSPACE_DIR:-/workspace}/node_modules
    environment:
      - CLAUDE_CONFIG_DIR=/home/node/.claude
      - NODE_OPTIONS=--max-old-space-size=4096
      - ANTHROPIC_API_KEY=${CLAUDE_API_KEY_A:-}
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4G
        reservations:
          cpus: "1"
          memory: 2G
    command: ["sleep", "infinity"]

  claude-b:
    image: claude-code-base:latest
    depends_on:
      - claude-a
    working_dir: ${WORKSPACE_DIR:-/workspace}
    stdin_open: true
    tty: true
    volumes:
      - ${PROJECT_DIR}:${WORKSPACE_DIR:-/workspace}
      - ${HOME}/.claude-state/account-b:/home/node/.claude
      - ${HOME}/.claude:/home/node/.claude-host:ro
      - ${HOME}/.config/gh:/home/node/.config/gh:ro
      - node_modules_b:${WORKSPACE_DIR:-/workspace}/node_modules
    environment:
      - CLAUDE_CONFIG_DIR=/home/node/.claude
      - NODE_OPTIONS=--max-old-space-size=4096
      - ANTHROPIC_API_KEY=${CLAUDE_API_KEY_B:-}
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4G
        reservations:
          cpus: "1"
          memory: 2G
    command: ["sleep", "infinity"]

volumes:
  node_modules_a:
  node_modules_b:
