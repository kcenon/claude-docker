# docker-compose.linux.yml
# Usage: docker compose -f docker-compose.yml -f docker-compose.linux.yml up

services:
  claude-a:
    user: "${UID}:${GID}"
    environment:
      - HOME=/home/node

  claude-b:
    user: "${UID}:${GID}"
    environment:
      - HOME=/home/node
