# docker-compose.worktree.yml
# Usage: docker compose -f docker-compose.yml -f docker-compose.worktree.yml up

services:
  claude-a:
    volumes:
      - ${PROJECT_DIR_A}:${WORKSPACE_DIR:-/workspace}   # Replaces shared PROJECT_DIR

  claude-b:
    volumes:
      - ${PROJECT_DIR_B}:${WORKSPACE_DIR:-/workspace}
